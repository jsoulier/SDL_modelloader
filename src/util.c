#include <SDL3/SDL.h>
#include <stb_image.h>

#include <assert.h>
#include <stddef.h>

SDL_GPUTexture* load_texture(SDL_GPUDevice* device, SDL_GPUCopyPass* copy_pass, const char* path)
{
    assert(device);
    assert(path);

    return NULL;
}